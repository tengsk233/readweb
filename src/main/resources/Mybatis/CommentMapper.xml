<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.first.mapper.CommentMapper">
    <insert id="Add">
        insert into bookweb.comment (book_id, username, comment, approval) values (#{book_id},#{username},#{comment},#{approval});
    </insert>
</mapper>
